package com.android.onlinefoodorderingapp.presentation.viewmodel

import io.kotest.core.spec.style.FunSpec

class SanityTest : FunSpec( {
    test("Sanity test") {
        // This is a simple sanity test to ensure that the testing framework is set up correctly.
        // You can replace this with actual tests for your ViewModel.
        assert(true)
    }
})